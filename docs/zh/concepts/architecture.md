# 架构

`lip` 用于管理工作区中已安装包的状态。

## 工作区状态

表示工作区当前状态，追踪安装了哪些包以及安装方式。
- **Packages**：已安装包集合。
- **Manifest**：当前项目的清单文件（如果存在）。

## 配置

用于管理 `lip` 工具本身的全局配置（如代理和默认行为）。
- **Properties**：配置项键值对（例如 `github_proxy`、`go_module_proxy`）。
