# CLI 概览

`lip` CLI 是管理包的主要接口，提供安装、更新和依赖管理等命令。

## 全局选项

| 选项 | 说明 |
| :--- | :--- |
| `--verbose` | 启用详细日志。 |
| `--json` | 以 JSON 格式输出结果。 |

## 常用命令

- `lip install`：安装包。
- `lip list`：列出已安装包。
- `lip init`：初始化新项目。
